"use client";

const AddToCart = () => {
  return (
    <button
      className="btn btn-primary"
      onClick={() => console.log("added to cart.")}
    >
      Add to Cart
    </button>
  );
};

export default AddToCart;
